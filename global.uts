import { toRefs, reactive } from 'vue'

export type StateModel = {
	isLogin : boolean // 是否登录
}
// 实例化为state
const state = reactive({ isLogin: false } as StateModel)
// 定义修改属性值的方法
const methods = {
	changeLogin() {
		state.isLogin = true
	}
}
export {
	state,
	methods
}