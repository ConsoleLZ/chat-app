# 一个uni-app的即时通讯项目
开发中...

## 聊天消息发送图片的基础逻辑梳理
1. 前端选择相册图片拿到File对象，并进行发送，先使用临时链接显示图片，这个时候这条消息处于发送状态（显示加载图标）
2. 服务端拿到File对象存储为静态文件，并且生成链接地址存储到redis中，并且返回给前端发送的状态，将真实地址作为参数传递
3. 前端收到服务端消息，判断是否发送成功，如果成功则替换图片的的临时链接为真实链接，发送失败则提示失败即可
4. 前面所有步骤都成功后，再进行用户的消息发送